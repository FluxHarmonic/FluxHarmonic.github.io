public/
.packages/
.sigil/
build/
# folio search index (local)
folio/.folio-index.db
folio/.folio-index.db-shm
folio/.folio-index.db-wal

# local MCP config (machine-specific absolute paths)
.mcp.json

# agent-orchestration machine-specific config (binary paths, relay/log dirs)
ops/.env
